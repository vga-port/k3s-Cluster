global:
  postgresql:
    auth:
      postgresPassword: "supersecret"
      username: "appuser"
      password: "apppassword"
      database: "appdb"

primary:
  persistence:
    enabled: true
    storageClass: longhorn
    size: 20Gi