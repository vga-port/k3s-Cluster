hostname: rancher.home.arpa
replicas: 1
bootstrapPassword: "CHANGEME-USE-A-REAL-PASSWORD"
ingress:
  extraAnnotations:
    kubernetes.io/ingress.class: traefik
  ingressClassName: traefik

  tls:
    source: rancher
letsEncrypt:
  email: "you@example.com"
  environment: production 
  ingress:
    class: traefik
agentTLSMode: system-store
resources:
  requests:
    cpu: 250m
    memory: 512Mi
